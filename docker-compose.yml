services:
  api:
    build:
      context: ./QuoteMapper.Api
      dockerfile: Dockerfile
    container_name: franca-seguros-api
    restart: unless-stopped
    ports:
      - "5060:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
